# Archived 3D Arena

This directory contains the inactive pre-pivot 3D arena prototype. It is retained as reference only.

The configured game, current tests, and all new development use the native-2D lab and side-scrolling battle under `lab/`, `battle/`, `kaiju/`, `enemies/`, and `levels/`.

Do not extend or instantiate these archived scenes from the active game.
